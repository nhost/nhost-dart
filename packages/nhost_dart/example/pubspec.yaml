name: nhost_dart_example
description: "nhost Dart sdk example"
private: true
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.14.0 <4.0.0"

dependencies:
  # Nhost packages
  nhost_dart:
    path: ../
