name: nhost_flutter_auth_sample
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0
environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: '>=4.0.0'
  shared_preferences: ^2.0.0

  # Nhost packages
  nhost_flutter_auth:
    path: ../

flutter:
  uses-material-design: true
