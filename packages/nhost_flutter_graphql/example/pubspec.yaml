name: nhost_flutter_graphql_example
publish_to: "none"
environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  graphql: ^5.0.0

  # Nhost packages
  nhost_flutter_graphql:
    path: ../

dev_dependencies:
  lints: ^1.0.0

flutter:
  uses-material-design: true
