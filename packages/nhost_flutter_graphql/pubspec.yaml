name: nhost_flutter_graphql
description: >
  Provides GraphQL clients to your Flutter application, automatically configured
  to work with Nhost
version: 0.1.0+2
homepage: https://nhost.io
repository: https://github.com/nhost/nhost-dart/packages/nhost_flutter_graphql

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  graphql_flutter: ^4.0.0

  # Nhost packages
  nhost_sdk: ^0.8.0+1
  nhost_flutter_auth: ^0.3.0+2
  nhost_graphql_adapter: ^0.6.0+2
dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.0
  pedantic: ^1.9.0
